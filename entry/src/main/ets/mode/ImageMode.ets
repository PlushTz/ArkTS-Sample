/**
 * Desc:
 * @author lijt
 * Created on 2024/3/9
 * Email: lijt@eetrust.com
 */
export class ImageMode {
   index: number
   url: string

  constructor(index: number, url: string) {
    this.index = index
    this.url = url
  }
}